## Notes

With this lab I got a refresher of Java. From python, typescript, and golang to this feels like going back to high school. Feelings aside, I was reminded today of pass-by-value, static arrays, and packages. First, I was reminded when creating some helper functions that values are copied to the method and they are not a refrence to that variable (even if they are objects). Next, when working on the for loops for searching through students I found myself getting errors because I was using students.length instead of studentCount. The for loops would loop through every empty item in the static array if a NullPointerException didn't stop it. Finally, going through each directory to reach the package was tideous so I will have to think of some way to make it easier for me. 
